## 掌握es6箭头函数
```
如果只有一个参数则小括号可以不写
如果返回只有一句话，则花括号可以不写
保留this指向的问题
```
## 能够申明一个es6的类
```
class
constructor 构造方法
fun(){}

继承
extends 
如果要继承父类的中的方法，并保留  重写 super();

小程序
	导出
	module.exports.名称 = 函数名称
	导入
	let 变量 = require(相对地址);

es6
export 可以在一个文件中申明多次
export default  它一般用于类的导入 一个文件中只能用一次

解析赋值
import {} from 相对地址
import * as obj from 相对地址
impor 变量 from 相对地址

小程序
导出
# 写法1
module.exports.变量 = 变量
# 写法2
module.exports  = {变量}
导入
var obj = require(相对地址)
```
## 能够使用wxs模块来过滤输出显示
```
小程序提供，不是JS的js
用它来做过滤wxml所用

xxxx.wxs
function fun(){}
导出
module.exports.fun = fun;

调用
wxml中
<wxs src=相对地址  module=变量 />

<view>{{变量.fun(js传过来的变量)}}</view>
```
## 能够了解常用的http请求类型
```
get put post delete 
```

## promise

```
没有剥夺函数return功能，所以我们可以用它来解决回调地狱的问题
a(){
    b(){
        c(){
            
        }
    }
}
a().then().then().then()

promise all方法

async await  es7解决
async function ajax(){
    await safsfsaf
}

vue for in 
vue for of 更好一些  es7
```

